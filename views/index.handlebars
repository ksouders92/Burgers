<div class="container">

<div class="container">
    <div class="row">
        <div class="col-lg-8 text center" id="top-bar">
            <h1 class="center-text">Welcome to Bob's Burgers! </h1>

        </div>
    </div>
    <div class="row">
        <div class="col-lg-8 text center" id="top-bar">
            <h2>Bob's Favorite Cheesiest Burgers of the Day</h2>
            <p>Which one of these fan favorites would you like to devour first?</p>
        </div>
    </div>
</div>
<br>
<br>
<div class="container">
    <div class="row">
        <div class="col-xs-6 text center">
            <div class="container">
                <h3>Available Burgers</h3>

                <hr> {{#each burger_data}} {{#unless this.devoured}} {{this.burger_name}}



                <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
                    <input type="hidden" name="burger_id" type="text" value={{this.id}}>

                    <button type="submit" class="btn btn-primary">
                        </strong>DEVOUR IT!</strong>
                    </button>

                </form>
                <br> {{/unless}} {{/each}}


            </div>

        </div>
        <div class="col-xs-3 text center">
            <div class="container">
                <h3>Create a New Burger</h3>
                <hr>
                <form action="/burgers/create" method="POST">
                    <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
                    <br>
                    <p>Enter a burger you'd like to create & submit!</p>

                    <button type="submit" class="btn btn-primary" id="text-enter-button"><strong>SUBMIT!</strong></button>
                </form>

            </div>
        </div>


        <div class="col-xs-3 text center">
            <div class="container">
                <h3>Devoured Burgers</h3>
                <hr> {{#each burger_data}} {{#if this.devoured}}
                <input class="form-control" type="text" placeholder="{{this.burger_name}}" readonly> {{/if}} {{/each}}

            </div>
        </div>
    </div>
</div>
</div>